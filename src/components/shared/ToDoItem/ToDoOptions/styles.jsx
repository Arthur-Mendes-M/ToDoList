import { styled } from "styled-components";
import { flexBox } from "../../../../styles/variables";

export const OptionsContainer = styled.div`
    ${flexBox(undefined, undefined, undefined, 1.5, "wrap")}
`